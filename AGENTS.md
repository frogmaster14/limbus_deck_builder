# AGENTS.md

이 문서는 림.딱 빌더 작업을 이어갈 때 Codex와 다른 코딩 에이전트가 우선 참고할 프로젝트 규칙이다.

## 프로젝트

- 서비스명은 `림.딱 빌더`, 보조 표기는 `L.T Deck TooL`을 쓴다.
- 정적 웹 앱이며 GitHub Pages로 배포한다.
- 저장소는 `frogmaster14/lt-deck-tool-beta`, 배포 주소는 `https://frogmaster14.github.io/lt-deck-tool-beta/`다.
- 현재 배포 흐름은 `main`에 커밋 후 푸시하고 GitHub Pages 반영을 확인하는 방식이다.

## 대화와 기록

- 사용자와의 대화는 한국어로 한다.
- 기본 진행 흐름은 `사용자 요청 → Codex의 의도 추론 확인 → 맞으면 진행, 아니면 재해석 → 목표 완료까지 반복`이다.
- 사용자의 표현이 거칠거나 모호해도 먼저 맥락을 바탕으로 의도를 찰떡같이 추론하고, 위험한 작업이면 그 추론을 확인받는다.
- 업데이트 기록은 짧고 직관적으로 쓴다. 예: `도감 기능 추가.`, `덱 저장 가능.`
- 업데이트 기록에는 `하였습니다` 같은 문장을 쓰지 않는다.
- 아주 사소한 UI 찐빠 수정은 버전을 올리거나 기록하지 않는다.
- 사용자에게 보여주는 안내문, 베타 안내, 도움말은 정중한 `습니다`체로 쓴다.
- 새 개발자 노트가 없는 버전은 가장 최근 개발자 노트를 유지해서 보여준다.

## 작업 방식

- 파일 수정 전에는 어떤 파일을 왜 바꾸는지 짧게 알린다.
- 수동 편집은 `apply_patch`를 쓴다.
- 사용자가 직접 넣은 이미지나 데이터 파일은 절대 임의 삭제하지 않는다.
- 이미지 정리, 이름 변경, 폴더 구조 변경은 실제 파일 존재 여부를 먼저 확인하고 진행한다.
- 기존 사용자 변경사항을 되돌리지 않는다.
- 배포나 릴리즈 작업 때 토큰, 인증값, 비공개 링크를 출력하지 않는다.

## 카파시식 작업 규칙

- 코딩 전에 애매한 요구사항, 전제, 선택지를 먼저 드러낸다.
- 의도가 불분명하거나 두 가지 이상으로 해석되면 조용히 찍지 말고 짧게 확인한다.
- 사용자의 명령이 정확하지 않더라도 먼저 의도를 추론하고, 그 추론이 맞는지 되묻거나 짧게 확인한 뒤 진행한다.
- 필요한 최소 코드로 해결한다. 요청받지 않은 기능, 추측성 설정, 일회용 추상화는 만들지 않는다.
- 기존 코드 스타일을 따른다. 마음에 들지 않는 주변 코드도 요청 없이는 리팩터링하지 않는다.
- 바뀐 모든 줄은 사용자의 요청과 직접 연결되어야 한다.
- 내가 만든 변경으로 생긴 미사용 코드나 고아 상태만 정리한다.
- 작업마다 성공 기준을 둔다. 가능한 경우 문법 검사, 데이터 검증, 화면 확인처럼 실제로 확인 가능한 방식으로 끝낸다.
- 간단한 한 줄 수정에는 과한 절차를 붙이지 않고, 위험하거나 넓은 변경일수록 더 조심한다.

## 데이터 규칙

- 카드 이미지에 적힌 정보가 원본이며, 필터 데이터는 베타 기준 보조 정보다.
- 수감자, 죄악, 공격 유형은 비교적 고정된 핵심 분류다.
- 인격 필터와 효과 필터는 피드백으로 계속 보정될 수 있다.
- EGO와 강화카드는 20장 덱 수에 포함하지 않는다.
- EGO와 강화카드는 강제 선택이 아니다.
- 20장 메인 덱은 카드와 수량만 저장해도 역산 가능한 정보가 많으므로 공유 코드는 중복 데이터를 줄인다.

## UI 규칙

- 기본 테마는 어두운 갈색과 금색 계열이다. 완전 검정 배경은 피한다.
- 패널 크기를 변경할 때는 내부 카드, 아이콘, 필터, 버튼 크기도 함께 조정해 빈 패널만 커지지 않게 한다.
- 피드백과 이용 방법 메뉴는 현재 숨김 상태다.
- 메인 화면의 진입 안내는 `더블클릭으로 들어갑니다.`를 쓴다.
- 카드 위에 마우스를 올리면 오른쪽 패널에서 크게 보여주는 흐름을 유지한다.
- 필터 아이콘은 이름 글자를 덧붙이지 않고 이미지 중심으로 보여준다.
- 카드 삽입은 도감과 같은 필터 UI를 가져와 클릭으로 선택하고, 오른쪽 패널의 삽입 버튼으로 넣는다.

## 확인 명령

수정 후 가능한 범위에서 아래 검사를 실행한다.

```powershell
& 'C:\Users\zotdo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --check 'script.js'
& 'C:\Users\zotdo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --check 'version-history.js'
git diff --check
if (Test-Path -LiteralPath 'scripts\validate-limbus-data.mjs') { & 'C:\Users\zotdo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' 'scripts\validate-limbus-data.mjs' }
```
